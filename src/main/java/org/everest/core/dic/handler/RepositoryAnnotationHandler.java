package org.everest.core.dic.handler;

import org.everest.core.dic.Instance;
import org.everest.decorator.Component;
import org.everest.decorator.Repository;

public class RepositoryAnnotationHandler implements ITypeAnnotationHandler<Repository> {

    @Override
    public void handle(Repository annotation, Instance instance) {

    }
}
