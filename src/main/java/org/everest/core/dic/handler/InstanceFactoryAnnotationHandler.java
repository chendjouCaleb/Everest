package org.everest.core.dic.handler;

import org.everest.core.dic.Instance;

public class InstanceFactoryAnnotationHandler implements IFactoryAnnotationHandler<org.everest.decorator.Instance> {

    @Override
    public void handle(org.everest.decorator.Instance annotation, Instance instance) {

    }
}
